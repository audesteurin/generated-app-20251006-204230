// This file is no longer used as the main application entry point.
// The root route now redirects to the dashboard within the AppLayout.
// It is kept to avoid breaking any potential imports but can be safely removed
// if no longer referenced anywhere.
export function HomePage() {
  return null;
}